(function() {

  'use strict';

  describe('experiments.views.upload module', function() {

    beforeEach(module('experiments.views.upload'));

    describe('Upload controller', function(){

      it('should ....', inject(function($controller) {
        //spec body
        var uploadCtrl = $controller('UploadCtrl');
        expect(uploadCtrl).toBeDefined();
      }));

    });
  });

})();
