(function() {

  'use strict';

  describe('experiments.views.todos module', function() {

    beforeEach(module('experiments.views.todos'));

    describe('todos controller', function(){

      it('should ....', inject(function($controller) {
        //spec body
        var todosCtrl = $controller('TodosCtrl');
        expect(todosCtrl).toBeDefined();
      }));

    });
  });

})();
