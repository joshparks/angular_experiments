(function() {

  'use strict';

  describe('experiments.views.albums module', function() {

    beforeEach(module('experiments.views.albums'));

    describe('albums controller', function(){

      it('should ....', inject(function($controller) {
        //spec body
        var albumsCtrl = $controller('AlbumsCtrl');
        expect(albumsCtrl).toBeDefined();
      }));

    });
  });

})();
