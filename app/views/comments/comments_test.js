(function() {

  'use strict';

  describe('experiments.views.comments module', function() {

    beforeEach(module('experiments.views.comments'));

    describe('comments controller', function(){

      it('should ....', inject(function($controller) {
        //spec body
        var commentsCtrl = $controller('CommentsCtrl');
        expect(commentsCtrl).toBeDefined();
      }));

    });
  });

})();
